[project]
name = "dealar-hermes-plugin"
version = "0.1.0"
description = "Hermes plugin bridge for Dealar deal intelligence"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
